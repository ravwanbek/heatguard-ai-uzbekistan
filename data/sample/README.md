# Sample data

The ready-to-use historical sample is stored at `data/processed/training_data.csv` (13,152 real Open-Meteo archive rows for 12 cities, 2023–2025). It is intentionally kept in `processed` because it is the exact reproducible model input. `models/metrics.json` records the source, split, row counts, and measured results.
